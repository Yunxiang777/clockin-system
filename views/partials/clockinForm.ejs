<h3>👋 歡迎，<%= employeeId %></h3>
<form id="clockinForm" method="POST" action="/clockin">
  <input type="hidden" id="lat" name="lat" />
  <input type="hidden" id="lng" name="lng" />
  <button type="submit">打卡</button>
</form>

<h2>🕒 打卡紀錄</h2>
<ul>
  <% records.forEach(r => { %>
  <li><%= new Date(r.time).toLocaleString() %>（IP：<%= r.ip %>）</li>
  <% }) %>
</ul>
